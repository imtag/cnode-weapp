<view class="loadmore">
  <view wx:if="{{ type === 'loading' }}">正在加载中...</view>
  <view wx:if="{{ type === 'nomore' }}">没有更多内容</view>
  <view wx:if="{{ type === 'error' }}" bind:tap="retry">网络错误，点击重试</view>
</view>